<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.landgate.infrastructure.dao.IUserDao">

    <resultMap id="UserResultMap" type="com.landgate.infrastructure.dao.po.UserPO">
        <id     column="id"                           property="id"/>
        <result column="email"                        property="email"/>
        <result column="email_verified"               property="emailVerified"/>
        <result column="password_hash"                property="passwordHash"/>
        <result column="role"                         property="role"/>
        <result column="balance"                      property="balance"/>
        <result column="concurrency"                  property="concurrency"/>
        <result column="status"                       property="status"/>
        <result column="username"                     property="username"/>
        <result column="notes"                        property="notes"/>
        <result column="totp_secret_encrypted"        property="totpSecretEncrypted"/>
        <result column="totp_enabled"                 property="totpEnabled"/>
        <result column="totp_enabled_at"              property="totpEnabledAt"/>
        <result column="signup_source"                property="signupSource"/>
        <result column="last_login_at"                property="lastLoginAt"/>
        <result column="last_active_at"               property="lastActiveAt"/>
        <result column="balance_notify_enabled"       property="balanceNotifyEnabled"/>
        <result column="balance_notify_threshold_type" property="balanceNotifyThresholdType"/>
        <result column="balance_notify_threshold"     property="balanceNotifyThreshold"/>
        <result column="balance_notify_extra_emails"  property="balanceNotifyExtraEmails"/>
        <result column="total_recharged"              property="totalRecharged"/>
        <result column="token_version"                property="tokenVersion"/>
        <result column="rpm_limit"                    property="rpmLimit"/>
        <result column="created_at"                   property="createdAt"/>
        <result column="updated_at"                   property="updatedAt"/>
        <result column="deleted_at"                   property="deletedAt"/>
    </resultMap>

    <!-- ==================== 基础 CRUD ==================== -->

    <select id="selectById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
            email, email_verified, password_hash, role, balance, concurrency, status, username, notes,
            totp_secret_encrypted, totp_enabled, totp_enabled_at,
            signup_source, last_login_at, last_active_at,
            balance_notify_enabled, balance_notify_threshold_type, balance_notify_threshold,
            balance_notify_extra_emails, total_recharged, token_version, rpm_limit,
            created_at, updated_at, deleted_at
        ) VALUES (
            #{email}, #{emailVerified}, #{passwordHash}, #{role}, #{balance}, #{concurrency}, #{status}, #{username}, COALESCE(#{notes}, ''),
            #{totpSecretEncrypted}, #{totpEnabled}, #{totpEnabledAt},
            #{signupSource}, #{lastLoginAt}, #{lastActiveAt},
            #{balanceNotifyEnabled}, #{balanceNotifyThresholdType}, #{balanceNotifyThreshold},
            COALESCE(#{balanceNotifyExtraEmails}, ''), #{totalRecharged}, #{tokenVersion}, #{rpmLimit},
            NOW(), NOW(), NULL
        )
    </insert>

    <update id="update">
        UPDATE users SET
            email = #{email}, email_verified = #{emailVerified}, password_hash = #{passwordHash}, role = #{role},
            balance = #{balance}, concurrency = #{concurrency}, status = #{status},
            username = #{username}, notes = #{notes},
            totp_secret_encrypted = #{totpSecretEncrypted}, totp_enabled = #{totpEnabled},
            totp_enabled_at = #{totpEnabledAt}, signup_source = #{signupSource},
            last_login_at = #{lastLoginAt}, last_active_at = #{lastActiveAt},
            balance_notify_enabled = #{balanceNotifyEnabled},
            balance_notify_threshold_type = #{balanceNotifyThresholdType},
            balance_notify_threshold = #{balanceNotifyThreshold},
            balance_notify_extra_emails = #{balanceNotifyExtraEmails},
            total_recharged = #{totalRecharged}, token_version = #{tokenVersion},
            rpm_limit = #{rpmLimit},
            deleted_at = #{deletedAt},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT * FROM users WHERE deleted_at IS NULL
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users WHERE deleted_at IS NULL
    </select>

    <!-- ==================== 业务查询 ==================== -->

    <select id="selectByEmail" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM users WHERE status = #{status} AND deleted_at IS NULL
    </select>

    <update id="updateBalance">
        UPDATE users SET balance = #{balance}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- ==================== 管理后台查询 ==================== -->

    <select id="selectBySearch" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE deleted_at IS NULL
        <if test="search != null and search != ''">
            AND (username LIKE CONCAT('%', #{search}, '%')
                 OR email LIKE CONCAT('%', #{search}, '%'))
        </if>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countBySearch" resultType="long">
        SELECT COUNT(*) FROM users
        WHERE deleted_at IS NULL
        <if test="search != null and search != ''">
            AND (username LIKE CONCAT('%', #{search}, '%')
                 OR email LIKE CONCAT('%', #{search}, '%'))
        </if>
    </select>

    <select id="countByCreatedAtAfter" resultType="long">
        SELECT COUNT(*) FROM users WHERE created_at &gt;= #{after} AND deleted_at IS NULL
    </select>

</mapper>
